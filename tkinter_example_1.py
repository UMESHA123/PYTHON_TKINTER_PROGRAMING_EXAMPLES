from tkinter import *
from tkinter.ttk import *

root=Tk()
#Tk() funtion are use to call the main windows and is stored in the root variable
# so it will help to edit the main windows
root.title("First tkinter example")
#the dot opareter are use to accsse the the root content and the that content
# title are use to give a titel to the root windows
root.mainloop() 